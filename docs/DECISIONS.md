# Decisions Log

История важных решений по проекту. Новые записи добавляются сверху.

---

## 2026-06-05 — Исправление ошибок Ahrefs Site Audit (ветка `seo/ahrefs-fixes`)

### Контекст
Ahrefs Site Audit (краул 04.06, Health 95): 4 ошибки. Главная —
«Page has links to broken page — 34 URLs». Плюс 4XX (2), Slow page (2),
Meta description too long (1).

### Корневая причина главной ошибки (диагностирована, подтверждена live)
Домен проксируется через **Cloudflare**. Включённая функция **Email Address
Obfuscation** (Scrape Shield) переписывает `mailto:office@paulburg.com` в футере
в ссылку `/cdn-cgi/l/email-protection#<hash>`, которая отдаёт **404**. Футер на
каждой странице → ~34 URL ссылаются на битую страницу. Проверено:
`curl /cdn-cgi/l/email-protection` → 404; в HTML главной и статей есть этот href.

### Что сделано
1. **Email-ссылка в футере** (`components/Footer.tsx`): вместо статического
   `mailto:` — кнопка, собирающая адрес из частей (`EMAIL_USER`/`EMAIL_DOMAIN`)
   в `onClick`. Литерал `mailto:office@paulburg.com` больше не попадает в
   SSR-HTML → Cloudflare нечего переписывать → 404-ссылка исчезает.
   *Альтернатива (отклонена):* выключить Email Obfuscation в Cloudflare — проще,
   но требует доступа к дашборду и оголяет адрес для скрейперов.
   *Trade-off:* без JS ссылка email не работает (приемлемо: адрес и не должен
   быть в HTML).
2. **Sitemap** (`app/sitemap.ts`): был хардкод из 2 URL. Теперь читает
   `content/posts/` и отдаёт `/`, `/services`, `/mentorship`, `/blog` + все
   посты (13 URL). Удалены мёртвые `public/sitemap.xml` и `public/robots.txt`
   (перекрывались `app/`-роутами).
3. **Meta description** (`app/page.tsx` 201→149, `app/services/page.tsx`
   185→143, `app/layout.tsx` 194→139 симв., ≤155; убран em-dash на главной).
4. **Ассеты:** `favicon.png`/`apple-icon.png` 640px/444 КБ → 96px/10 КБ и
   180px/35 КБ; удалены неиспользуемые `hero.jpg` (8.4 МБ) и `hero.png`
   (главная использует `hero.webp`). Бэкап иконок: `/tmp/paulburg-icon-backups/`.

### Не проверено без деплоя
Очистка ошибок Ahrefs возможна только после деплоя на Vercel + повторного
краула. Локально (next dev) подтверждено: в HTML главной нет `mailto`/`cdn-cgi`,
sitemap.xml отдаёт 13 URL, длины description 149/143. Второй URL из «4XX page (2)»
стоит сверить в детализации Ahrefs (ожидается второй хэш email-protection).

---

## 2026-05-29 — Попытка редизайна (v2, v3) — ЗАМОРОЖЕНА, сайт оставлен как есть

### Контекст
Друг (профессиональный дизайнер/кодер) посмотрел на сайт и сказал, что он
похож на «AI-slop»: видны стереотипные, повторяющиеся, generic-приёмы, и текст
тяжело читать. Запрос: переделать так, чтобы был «вау-эффект» и ощущение
аутентичности, не похоже на генерацию AI.

### Что было сделано (две попытки, обе НЕ приняты)

**Попытка 1 — ветка `redesign/v2-editorial-restraint` (5 коммитов).**
Подход «editorial restraint» = вычитание: убраны мигающий курсор, полая
обводка фамилии (BurgMark), `§ NN` маркеры секций, координаты места, зерно
(grain), ambient-градиенты, угловые L-скобки, terminal-точки, mouse-glow;
шрифты сведены с 5 до 2; убраны scroll-анимации; дисциплина по золотому цвету.

Результат: **отклонено пользователем.** Вердикт — стало не аутентичнее, а
**беднее**. Вывод: «чистый минималистичный тёмный сайт + serif + один акцент»
сам по себе является generic AI-выходом. Вычитание заменило один вид «слопа»
другим, более скучным.

**Попытка 2 — ветка `redesign/v3-refine` (1 коммит `f85ce51`).**
Подход «огранить оригинал» = сохранить весь характер, починить только крафт
(читаемость, повторяемость, иерархию). На hero: питч сделан roman вместо
курсива и крупнее (20px), координаты `16°N 108°E` убраны (но «Da Nang» оставлен),
мигающий курсор заменён на статичную золотую точку, hollow-stroke «Burg» сохранён.

Результат: **отклонено пользователем.** Вердикт — разницы почти не видно;
замена мигающего курсора на точку «скучнее, чем было». Сессия заморожена.

### Итоговое решение
**Сайт оставлен как есть — на ветке `main` (текущая прод-версия, оригинал).**
Ничего не смержено в `main`, ничего не задеплоено. Рабочее дерево возвращено
на `main`.

### Ветки сохранены как запись (можно удалить позже)
- `redesign/v2-editorial-restraint` — полное «минималистичное» вычитание (5 фаз).
- `redesign/v3-refine` — точечная огранка hero (1 коммит).

Удалить, когда перестанут быть нужны:
`git branch -D redesign/v2-editorial-restraint redesign/v3-refine`

### Главный урок для будущей сессии
1. **AI-slop ≠ наличие эффектов.** Это отсутствие точки зрения. Убирать декор
   «в ноль» — ошибка: получается ещё более generic результат.
2. **Правки должны быть видимы.** Пользователь дважды сказал «не вижу разницы».
   Мелкие типографические сдвиги (kerning, +2px, roman вместо italic) по
   отдельности не читаются как изменение. Если делать — то заметные шаги.
3. **Замена клише на «более спокойное клише» не работает** (курсор → точка).
   Нужна либо настоящая авторская идея, либо не трогать вовсе.
4. Прежде чем тратить фазы реализации — **показать визуальные макеты**
   (несколько РАЗНЫХ направлений рядом), потому что пользователь не выбирает
   направление «по словам», ему нужно видеть результат.
5. Реальные нерешённые претензии (по словам друга): (а) читаемость текста,
   (б) повторяющийся/штампованный ритм 8 одинаковых секций-eyebrow. Если
   возвращаться — это две настоящие задачи, но решать их надо ЗАМЕТНО и с
   сохранением характера, а не вычитанием.

### Что НЕ трогалось ни в одной попытке (работает, прод-версия)
Многоязычность (`context/LanguageContext.tsx`, `components/Providers.tsx`),
модалки заявок (`IntakeModal.tsx`, `MentorshipIntakeModal.tsx`), блог-система
(`app/blog/`, `lib/posts.ts`, MDX, `auto_publish.py`), API-роуты (`app/api/`),
SEO-метаданные и JSON-LD (`app/layout.tsx`), тема dark/light, формы и вебхуки.
